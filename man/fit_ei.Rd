% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_ei.R
\name{fit_ei}
\alias{fit_ei}
\title{Fit an Ecological Inference Model}
\usage{
fit_ei(
  data,
  method = c("rxc", "iter", "goodman"),
  race_cols = c("white", "black"),
  cand_cols = c("cand_A", "cand_B"),
  totals_col = "total",
  lambda1 = 4,
  lambda2 = 2,
  sample = 50000,
  burnin = 10000,
  thin = 25,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{data}{A precinct-level data frame.}

\item{method}{One of \code{"rxc"}, \code{"iter"}, \code{"goodman"}.}

\item{race_cols}{Character vector of column names for race \strong{counts} (used
by \code{"rxc"}) and proportions (used by \code{"iter"}). Provide both via
\code{race_count_cols} and \code{race_pct_cols} if your data uses different names.
By default, \code{race_cols = c("white", "black")} and the function looks for
\code{n_white}, \code{n_black}, \code{pct_white}, \code{pct_black}.}

\item{cand_cols}{Character vector for candidate \strong{counts} (\code{cand_A},
\code{cand_B}) and proportions (\code{pct_A}, \code{pct_B}).}

\item{totals_col}{Name of the total-voters column.}

\item{lambda1, lambda2}{Dirichlet concentration hyperparameters for
\code{ei.MD.bayes}. Lower values = less shrinkage. Defaults \code{lambda1 = 4},
\code{lambda2 = 2} match eiPack defaults.}

\item{sample, burnin, thin}{MCMC settings for RxC. Defaults are modest; raise
for production runs.}

\item{verbose}{Logical; print progress.}

\item{...}{Additional arguments passed to the underlying method.}
}
\value{
A list with elements:
\describe{
\item{method}{The method used.}
\item{estimates}{For \code{"rxc"}: a list with \code{point}, \code{lower}, \code{upper},
\code{draws}. For \code{"iter"} and \code{"goodman"}: a matrix of point estimates
(rows = groups, cols = candidates).}
\item{fit}{The raw fit object.}
}
}
\description{
Unified wrapper around three ecological inference methods: RxC Bayesian
(via \code{\link[eiPack:ei.MD.bayes]{eiPack::ei.MD.bayes()}}), iterative 2x2 (via \code{\link[eiCompare:ei_iter]{eiCompare::ei_iter()}}),
and Goodman's ecological regression (a simple linear regression).
}
\details{
Defaults assume the column naming conventions produced by
\code{\link[=simulate_election]{simulate_election()}}: race columns \code{n_white}, \code{n_black}, \code{pct_white},
\code{pct_black}, candidate columns \code{cand_A}, \code{cand_B}, \code{pct_A}, \code{pct_B}, and
\code{total}. Override via \code{race_cols}, \code{cand_cols}, \code{totals_col} for real data.
}
\examples{
# --- Goodman's ecological regression (fast, no MCMC) ---
dat <- simulate_election(n_precincts = 40, seed = 1)
fit_g <- fit_ei(dat, method = "goodman")
fit_g$estimates
# Compare to embedded truth
attr(dat, "true_support")

\dontrun{
# --- RxC Bayesian with default prior (requires eiPack) ---
fit_rxc <- fit_ei(dat, method = "rxc",
                  sample = 20000, burnin = 5000, thin = 10)
fit_rxc$estimates$point   # posterior means
fit_rxc$estimates$lower   # 2.5\% credible bound
fit_rxc$estimates$upper   # 97.5\% credible bound

# --- RxC with calibrated (weaker) prior ---
fit_cal <- fit_ei(dat, method = "rxc",
                  lambda1 = 0.5, lambda2 = 0.5,
                  sample = 20000, burnin = 5000, thin = 10)
fit_cal$estimates$point

# --- Iterative 2x2 EI (requires eiCompare) ---
fit_iter <- fit_ei(dat, method = "iter")
fit_iter$estimates

# --- Score all methods against truth ---
truth <- attr(dat, "true_support")
score_estimates(fit_g$estimates, truth)
score_estimates(fit_rxc$estimates$point, truth)
score_estimates(fit_cal$estimates$point, truth)
}

}
