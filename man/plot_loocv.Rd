% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_loocv}
\alias{plot_loocv}
\title{Plot Leave-One-Out Cross-Validation Results}
\usage{
plot_loocv(loocv_result, type = c("summary", "precinct"), title = NULL)
}
\arguments{
\item{loocv_result}{Output of \code{\link[=loo_cv]{loo_cv()}}.}

\item{type}{One of \code{"summary"} (bar chart of RMSE/MAE) or \code{"precinct"}
(dot plot of per-precinct errors). Default \code{"summary"}.}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualize LOO-CV results from \code{\link[=loo_cv]{loo_cv()}}. Two display types are available:
a summary bar chart of RMSE and MAE by method, and a per-precinct error
dot plot.
}
\examples{
# --- Fast Goodman-only example ---
dat <- simulate_election(n_precincts = 30, seed = 42)
cv <- loo_cv(dat, methods = "goodman", verbose = FALSE)
plot_loocv(cv, type = "summary")
plot_loocv(cv, type = "precinct")

\dontrun{
# --- Compare methods ---
cv2 <- loo_cv(dat, methods = c("goodman", "rxc_default"),
              sample = 10000, burnin = 2000, thin = 5)
plot_loocv(cv2, type = "summary")
plot_loocv(cv2, type = "precinct")
}

}
