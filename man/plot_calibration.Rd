% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_calibration}
\alias{plot_calibration}
\title{Plot Calibration Results}
\usage{
plot_calibration(calib_result, loss = NULL, title = NULL)
}
\arguments{
\item{calib_result}{Output of \code{\link[=calibrate_rxc]{calibrate_rxc()}}.}

\item{loss}{Character. Which loss to plot: \code{"rmse"}, \code{"mae"}, or
\code{"max_err"}. Defaults to the loss used during calibration
(\code{calib_result$loss}).}

\item{title}{Optional character string for the plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualize the RMSE (or other loss) surface from \code{\link[=calibrate_rxc]{calibrate_rxc()}}.
In 1D mode, produces a line plot of loss versus lambda. In 2D mode,
produces a heatmap of the loss surface across the
\eqn{(\lambda_1, \lambda_2)} grid with the optimal cell highlighted.
}
\examples{
\dontrun{
truth <- matrix(c(0.80, 0.20, 0.15, 0.85), nrow = 2, byrow = TRUE,
                dimnames = list(c("white", "black"),
                                c("cand_A", "cand_B")))
dat <- simulate_election(n_precincts = 40, true_support = truth, seed = 1)

# 1D calibration + plot
cal_1d <- calibrate_rxc(dat, truth,
                        lambda_grid = c(0.1, 0.5, 1, 2, 4),
                        sample = 10000, burnin = 2000, thin = 5)
plot_calibration(cal_1d)

# 2D calibration + heatmap
cal_2d <- calibrate_rxc(dat, truth,
                        lambda1_grid = c(0.1, 0.5, 1, 4),
                        lambda2_grid = c(0.25, 1, 2, 8),
                        sample = 10000, burnin = 2000, thin = 5)
plot_calibration(cal_2d)
}

}
