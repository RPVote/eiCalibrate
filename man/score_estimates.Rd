% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score_estimates.R
\name{score_estimates}
\alias{score_estimates}
\title{Score Ecological Inference Estimates Against Ground Truth}
\usage{
score_estimates(estimate_matrix, truth_matrix)
}
\arguments{
\item{estimate_matrix}{Numeric matrix (rows = groups, cols = candidates) of
point estimates. For RxC output from \code{\link[=fit_ei]{fit_ei()}}, pass \code{fit$estimates$point}.}

\item{truth_matrix}{Numeric matrix of the same shape, with rows summing to 1.}
}
\value{
A list with \code{rmse}, \code{mae}, \code{max_err}, and \code{cell_errors} (the signed
element-wise differences).
}
\description{
Computes RMSE, MAE, and max cell-wise error between an estimated
group-by-candidate matrix and a known truth matrix.
}
\examples{
# --- Basic scoring ---
truth <- matrix(c(0.80, 0.20, 0.15, 0.85), nrow = 2, byrow = TRUE,
                dimnames = list(c("white", "black"),
                                c("cand_A", "cand_B")))

# Simulated estimate close to truth
set.seed(1)
est_good <- truth + matrix(rnorm(4, 0, 0.03), 2, 2)
score_estimates(est_good, truth)

# Simulated estimate far from truth (50-50 across the board)
est_bad <- matrix(0.5, 2, 2,
                  dimnames = list(c("white", "black"),
                                  c("cand_A", "cand_B")))
score_estimates(est_bad, truth)

# --- Using with fit_ei output ---
dat <- simulate_election(n_precincts = 40, true_support = truth, seed = 1)
fit_g <- fit_ei(dat, method = "goodman")
score_estimates(fit_g$estimates, truth)

}
