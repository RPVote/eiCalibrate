% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate_rxc.R
\name{calibrate_rxc}
\alias{calibrate_rxc}
\title{Calibrate the RxC Dirichlet Prior Against Known Benchmarks}
\usage{
calibrate_rxc(
  calib_data,
  calib_truth,
  lambda1_grid = NULL,
  lambda2_grid = NULL,
  lambda_grid = c(0.1, 0.25, 0.5, 1, 2, 4),
  n_reps = 1,
  loss = c("rmse", "mae", "max_err"),
  ...
)
}
\arguments{
\item{calib_data}{Precinct-level data frame for the calibration election.}

\item{calib_truth}{Numeric matrix of known group-conditional candidate
support (rows sum to 1).}

\item{lambda1_grid}{Numeric vector of \code{lambda1} values to evaluate. If
\code{NULL} (default), falls back to \code{lambda_grid} (1D mode).}

\item{lambda2_grid}{Numeric vector of \code{lambda2} values to evaluate. If
\code{NULL} (default), falls back to \code{lambda_grid} (1D mode).}

\item{lambda_grid}{Numeric vector for backward-compatible 1D mode where
\code{lambda1 = lambda2} at each grid point. Ignored if both \code{lambda1_grid}
and \code{lambda2_grid} are supplied.}

\item{n_reps}{Integer. Number of replications per grid point (to smooth
MCMC noise). Point estimates are averaged across reps before scoring.
Default 1.}

\item{loss}{One of \code{"rmse"}, \code{"mae"}, \code{"max_err"}. Loss function for
selecting the best prior.}

\item{...}{Additional arguments passed to \code{\link[=fit_ei]{fit_ei()}} (e.g. \code{sample},
\code{burnin}, \code{thin}, \code{race_cols}, \code{cand_cols}, \code{totals_col}).}
}
\value{
A list with:
\describe{
\item{summary}{Data frame of results across the grid. In 2D mode,
columns include \code{lambda1}, \code{lambda2}, \code{rmse}, \code{mae}, \code{max_err}.}
\item{best_lambda1}{The optimal \code{lambda1}.}
\item{best_lambda2}{The optimal \code{lambda2}.}
\item{best_lambda}{(1D mode only) The single optimal lambda. In 2D mode,
this is \code{NULL}.}
\item{best_fit}{The fit (or averaged estimates) at the optimal point.}
\item{rmse_matrix}{(2D mode only) Matrix of RMSE values with \code{lambda1}
on rows and \code{lambda2} on columns.}
\item{loss}{The loss function used.}
\item{mode}{\code{"1D"} or \code{"2D"} indicating which calibration was run.}
}
}
\description{
Fits \code{\link[eiPack:ei.MD.bayes]{eiPack::ei.MD.bayes()}} (via \code{\link[=fit_ei]{fit_ei()}}) across a grid of Dirichlet
concentration values on a \emph{calibration} dataset where the true
group-by-candidate support is known. Returns the prior setting that
minimizes the chosen loss against ground truth. The calibrated values are
then meant to be applied (via \code{\link[=fit_ei]{fit_ei()}}) to a \emph{separate target election}
from the same jurisdiction/context.
}
\details{
By default, the function performs a \strong{2D joint calibration} over separate
grids for \code{lambda1} (candidate support concentration) and \code{lambda2}
(row-marginal concentration). If only \code{lambda_grid} is supplied, both
parameters are tied to the same value (1D sweep) for backward
compatibility.

This is empirical Bayes calibration. It is only defensible when (a) the
calibration ground truth is genuinely credible (exit polls, validated
surveys, homogeneous precinct analysis), and (b) the calibration and target
elections share comparable polarization dynamics. \strong{Do not} tune the prior
to ground truth from the \emph{same} election you intend to analyze - that is
circular.
}
\examples{
\dontrun{
truth <- matrix(c(0.80, 0.20, 0.15, 0.85), nrow = 2, byrow = TRUE,
                dimnames = list(c("white", "black"),
                                c("cand_A", "cand_B")))
calib <- simulate_election(n_precincts = 40, true_support = truth, seed = 1)

# --- 2D joint calibration (recommended) ---
calib_2d <- calibrate_rxc(calib, truth,
                          lambda1_grid = c(0.05, 0.1, 0.25, 0.5, 1, 2, 4),
                          lambda2_grid = c(0.05, 0.25, 0.5, 1, 2, 4, 8),
                          n_reps = 3,
                          sample = 20000, burnin = 5000, thin = 10)
cat("Best (lambda1, lambda2):", calib_2d$best_lambda1, ",",
    calib_2d$best_lambda2, "\n")
print(calib_2d$rmse_matrix)

# --- 1D calibration (backward compatible) ---
calib_1d <- calibrate_rxc(calib, truth,
                          lambda_grid = c(0.1, 0.25, 0.5, 1, 2, 4),
                          sample = 20000, burnin = 5000, thin = 10)
cat("Best lambda:", calib_1d$best_lambda, "\n")
}

}
