% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_sensitivity}
\alias{plot_sensitivity}
\title{Plot Sensitivity Sweep Results}
\usage{
plot_sensitivity(sweep_result, cell, title = NULL)
}
\arguments{
\item{sweep_result}{Output of \code{\link[=sensitivity_sweep]{sensitivity_sweep()}}.}

\item{cell}{Character identifying the cell to plot. Either a single string
in \code{"group_candidate"} format (e.g., \code{"white_cand_A"}) or a length-2
vector \code{c("white", "cand_A")}.}

\item{title}{Optional plot title.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualize how a specific group-conditional support estimate varies across
prior strength, using output from \code{\link[=sensitivity_sweep]{sensitivity_sweep()}}.
}
\examples{
\dontrun{
dat <- simulate_election(n_precincts = 40, seed = 1)
sweep <- sensitivity_sweep(dat,
                           lambda_grid = c(0.1, 0.5, 1, 2, 4),
                           sample = 10000, burnin = 2000, thin = 5)
plot_sensitivity(sweep, cell = "black_cand_A")
plot_sensitivity(sweep, cell = c("white", "cand_B"))
}

}
