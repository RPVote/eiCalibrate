% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sweep_and_compare.R
\name{sensitivity_sweep}
\alias{sensitivity_sweep}
\title{Sweep RxC Prior Strength Without a Ground-Truth Benchmark}
\usage{
sensitivity_sweep(
  data,
  lambda1_grid = NULL,
  lambda2_grid = NULL,
  lambda_grid = c(0.1, 0.25, 0.5, 1, 2, 4),
  ...
)
}
\arguments{
\item{data}{Precinct-level data.}

\item{lambda1_grid}{Numeric vector of \code{lambda1} values. If \code{NULL},
falls back to \code{lambda_grid} (1D mode).}

\item{lambda2_grid}{Numeric vector of \code{lambda2} values. If \code{NULL},
falls back to \code{lambda_grid} (1D mode).}

\item{lambda_grid}{Numeric vector for 1D mode (lambda1 = lambda2).}

\item{...}{Passed to \code{\link[=fit_ei]{fit_ei()}}.}
}
\value{
A list with \code{all_results} (list of fits), \code{point_estimates} (an
array of group-by-candidate estimates), \code{lambda_grid} (1D) or
\code{lambda1_grid}/\code{lambda2_grid} (2D), and \code{mode}.
}
\description{
Like \code{\link[=calibrate_rxc]{calibrate_rxc()}} but without scoring against truth - returns point
estimates across a grid of prior strengths so you can assess sensitivity.
Use this when you have no calibration data but want to understand how much
the prior is driving your results.
}
\details{
By default, performs a 1D sweep where \code{lambda1 = lambda2}. Supply separate
\code{lambda1_grid} and \code{lambda2_grid} for a 2D sensitivity analysis.
}
\examples{
\dontrun{
dat <- simulate_election(n_precincts = 40, seed = 1)

# 1D sweep (lambda1 = lambda2)
sweep_1d <- sensitivity_sweep(dat,
                              lambda_grid = c(0.1, 0.5, 1, 2, 4),
                              sample = 20000, burnin = 5000, thin = 10)
sweep_1d$point_estimates

# 2D sweep (independent lambda1 and lambda2)
sweep_2d <- sensitivity_sweep(dat,
                              lambda1_grid = c(0.1, 0.5, 1, 4),
                              lambda2_grid = c(0.25, 1, 2, 8),
                              sample = 20000, burnin = 5000, thin = 10)
sweep_2d$point_estimates  # 4D array: [group, candidate, lambda1, lambda2]
}

}
