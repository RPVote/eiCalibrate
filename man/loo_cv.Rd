% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loo_cv.R
\name{loo_cv}
\alias{loo_cv}
\title{Leave-One-Out Cross-Validation for Ecological Inference Methods}
\usage{
loo_cv(
  data,
  methods = c("goodman", "rxc_default"),
  race_cols = c("white", "black"),
  cand_cols = c("cand_A", "cand_B"),
  totals_col = "total",
  dem_col_idx = 1L,
  calibrated_lambda1 = NULL,
  calibrated_lambda2 = NULL,
  sample = 25000,
  burnin = 5000,
  thin = 10,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{data}{Precinct-level data frame. Must contain race count columns
(\verb{n_<race>}), race proportion columns (\verb{pct_<race>}), candidate count
columns, candidate proportion columns (\verb{pct_<cand>}), and a totals column.}

\item{methods}{Character vector of methods to compare. Options:
\code{"goodman"} (Goodman's ecological regression),
\code{"rxc_default"} (RxC with default priors \eqn{\lambda_1 = 4, \lambda_2 = 2}),
\code{"rxc_calibrated"} (RxC with user-specified priors),
\code{"iter"} (iterative 2x2 EI via eiCompare).
Default: \code{c("goodman", "rxc_default")}.}

\item{race_cols}{Character vector of race group base names (e.g.,
\code{c("white", "black")}). The function expects columns named \verb{n_<race>}
and \verb{pct_<race>} in the data.}

\item{cand_cols}{Character vector of candidate column names for vote counts
(e.g., \code{c("cand_A", "cand_B")}). The function expects corresponding
proportion columns named \verb{pct_<cand>} (with \code{cand_} prefix replaced by
\code{pct_}).}

\item{totals_col}{Name of the total-voters column. Default \code{"total"}.}

\item{dem_col_idx}{Integer. Which candidate column (1-indexed) to use as
the prediction target. Default \code{1} (first candidate). The LOO-CV predicts
this candidate's aggregate vote share in each held-out precinct.}

\item{calibrated_lambda1}{Numeric. \eqn{\lambda_1} for \code{"rxc_calibrated"}.
Required if \code{"rxc_calibrated"} is in \code{methods}.}

\item{calibrated_lambda2}{Numeric. \eqn{\lambda_2} for \code{"rxc_calibrated"}.
Defaults to \code{calibrated_lambda1} if not supplied.}

\item{sample}{Integer. MCMC sample size for RxC fits. Default \code{25000}.
Shorter chains are appropriate for CV since only point estimates are needed.}

\item{burnin}{Integer. MCMC burn-in for RxC fits. Default \code{5000}.}

\item{thin}{Integer. MCMC thinning interval for RxC fits. Default \code{10}.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[=fit_ei]{fit_ei()}}.}
}
\value{
A list with elements:
\describe{
\item{precinct_errors}{Data frame with columns: \code{precinct} (row index),
\code{method}, \code{predicted} (predicted vote share), \code{actual} (observed vote
share), \code{error} (predicted - actual).}
\item{summary}{Data frame with columns: \code{method}, \code{rmse}, \code{mae},
\code{n_success} (folds that produced estimates), \code{n_failed} (folds where
fitting failed).}
\item{settings}{List recording the arguments used.}
}
}
\description{
Evaluates the out-of-sample predictive accuracy of ecological inference
methods by iteratively holding out each precinct, estimating
group-conditional candidate support from the remaining precincts, and
predicting the held-out precinct's aggregate vote share. The method whose
estimated support rates best reproduce observed precinct-level outcomes is,
in a predictive sense, best calibrated to the data.
}
\details{
The prediction for held-out precinct \eqn{p} is:
\deqn{\hat{V}_p^{\text{Dem}} = \sum_{r=1}^{R} \pi_{pr} \cdot
  \hat{\theta}_{r,\text{Dem}}^{(-p)}}
where \eqn{\pi_{pr}} is the share of group \eqn{r} in precinct \eqn{p}'s
voting-age population, and \eqn{\hat{\theta}_{r,\text{Dem}}^{(-p)}} is
the estimated support for the target candidate among group \eqn{r}, fitted
on all precincts except \eqn{p}.
}
\examples{
# --- Fast example with Goodman ER only (no MCMC) ---
dat <- simulate_election(n_precincts = 30, seed = 42)
cv <- loo_cv(dat, methods = "goodman")
cv$summary
head(cv$precinct_errors)

\dontrun{
# --- Compare Goodman ER vs RxC Default ---
cv2 <- loo_cv(dat, methods = c("goodman", "rxc_default"),
              sample = 10000, burnin = 2000, thin = 5)
cv2$summary

# --- Include calibrated RxC ---
cv3 <- loo_cv(dat,
              methods = c("goodman", "rxc_default", "rxc_calibrated"),
              calibrated_lambda1 = 0.5, calibrated_lambda2 = 0.25,
              sample = 10000, burnin = 2000, thin = 5)
cv3$summary

# --- Visualize results ---
plot_loocv(cv3, type = "summary")
plot_loocv(cv3, type = "precinct")
}

}
